# Datos RAW (NO versionar): originales + extraidos parquet
data_mundial/
data/public/
data/parquet/pff/
data/parquet/wyscout/
data/parquet/statsbomb/
# Caches derivadas SI se versionan (output regenerable del pipeline,
# pero util tenerlas trackeadas para reproducir resultados sin recomputar):
#   data/parquet/derived/    -> SI versionar
#   cache/vaep/              -> SI versionar (compute_features/labels VAEP)

# Documentacion interna del proyecto
docs/
CLAUDE.md

# Outputs (pcj_table.parquet final + pcj_aux/) -> SI versionar (output canonico
# del pipeline para que cualquiera pueda inspeccionar resultados sin recomputar)

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Pytest / mypy / ruff cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/
.nox/

# Notebooks (checkpoints)
.ipynb_checkpoints/
*-checkpoint.ipynb

# Entornos virtuales
.venv/
venv/
env/
.env
.envrc

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Modelos grandes (pero los del pipeline derivado SI se versionan dentro
# de data/parquet/derived/*/model/)
*.pt
*.ckpt

# Logs
*.log
logs/

# Cache socceraction StatsBombLoader (raw API cache, no necesario)
catboost_info/

# Scripts operativos (monitor con IP/puerto del pod, no metodologia)
scripts/

# Claude Code session metadata
.claude/

# cate_nuts.pkl es 409 MB (samples NUTS 4 chains x 1000 samples), supera
# limite GitHub 100MB. Regenerable corriendo M14 con panel_delta cacheado.
data/parquet/derived/cate/model/cate_nuts.pkl
